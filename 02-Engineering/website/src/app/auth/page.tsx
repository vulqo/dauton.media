import AuthPage from '@/components/AuthPage';

export const metadata = {
  title: 'Entrar — Dauton Media',
  description: 'Inicia sesión o crea una cuenta en Dauton Media.',
};

export default function AuthRoute() {
  return <AuthPage />;
}
