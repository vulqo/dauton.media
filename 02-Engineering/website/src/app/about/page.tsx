import AboutPage from '@/components/AboutPage';

export const metadata = {
  title: 'Sobre — Dauton Media',
  description: 'Sobre Dauton Media: archivo editorial del rap hispanohablante.',
};

export default function AboutRoute() {
  return <AboutPage />;
}
