/** Stage 6: YouTube transcript extraction + NER + dedup. Implementation: Sprint 8. */
export async function runStage6Transcripts(_channelIds: string[]): Promise<void> {
  throw new Error('Stage 6 not implemented yet (Sprint 8)');
}
